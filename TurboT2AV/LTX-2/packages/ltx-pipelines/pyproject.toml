[project]
name = "ltx-pipelines"
version = "1.0.0"
description = "Minimal model loading utilities for TurboT2AV inference"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["ltx-core", "torch"]

[build-system]
requires = ["uv_build>=0.9.8,<0.10.0"]
build-backend = "uv_build"
